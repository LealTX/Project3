{
  "name": "week20",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^4.1.0",
    "dotenv": "^6.2.0",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-materialize": "^2.6.0",
    "react-scripts": "2.1.3",
    "spotify-web-api-node": "^4.0.0"
  },
  "scripts": {
    "start": "concurrently \"cd client && yarn start\" \"cd auth-server && node authorization_code/app.js\"",
    "build": "yarn build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "yarn build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
